apiVersion: v1
kind: Service
metadata:
  name: adapter-rest-svc-node-port
  labels:
    app: adapter-rest-svc-node-port
spec:
  type: NodePort
  selector:
    app: adapter-rest-pod
  ports:
    - port: 8081
      protocol: TCP
      targetPort: 8081
      nodePort: 31081